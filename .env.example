# Supabase Configuration
# Get these from your Supabase project settings
VITE_SUPABASE_URL=https://your-project.supabase.co

# Use publishable key (recommended - format: sb_publishable_...)
# Or anon key for backward compatibility (JWT format)
VITE_SUPABASE_KEY=your-publishable-key-or-anon-key

# Optional: Force demo mode for local development
# Set to 'true' to bypass Supabase auth and use localStorage-only mode
# Useful for frontend development without needing Supabase credentials
VITE_FORCE_DEMO_MODE=false

# Optional: Azure Functions Backend URL for production
# Leave empty for local development (will use /api which is proxied to localhost:7071)
# For GitHub Pages deployment, set to your Azure Functions base URL:
# VITE_API_BASE_URL=https://your-function-app.azurewebsites.net/api
VITE_API_BASE_URL=

# Optional: Base path for deployment (default: /)
# Use /switch-library/ for GitHub Pages
VITE_BASE_PATH=/

# Optional: Debug logging for specific user ID
# When set, enables detailed console logging only for this user
# Useful for debugging production issues without exposing logs for all users
# Set to the user's ID (visible in the user profile or auth state)
# VITE_DEBUG_USER_ID=your-user-id-here
VITE_DEBUG_USER_ID=

# Optional: Admin user allowlist (additional security layer)
# Comma-separated list of user IDs that are allowed admin access
# Provides an additional security check beyond database account_level
# Even if account_level=admin in database, user must be in this list
# Leave empty to disable allowlist (database-only check)
# VITE_ADMIN_ALLOWLIST=uuid1,uuid2,uuid3
VITE_ADMIN_ALLOWLIST=

# Optional: Admin function key for backend database statistics
# Required to access the /api/admin/database-stats endpoint
# Get this from Azure Portal -> Function App -> Functions -> GetDatabaseStatsAdmin -> Function Keys
# Enables the backend database statistics section in the admin dashboard
# VITE_ADMIN_FUNCTION_KEY=your-function-key-here
VITE_ADMIN_FUNCTION_KEY=
